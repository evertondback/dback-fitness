export const ANATOMY_STUDIO_NAV_FIX_JS=`(()=>{document.addEventListener('click',e=>{const b=e.target.closest('.rStudioBottom button');if(!b)return;e.preventDefault();e.stopImmediatePropagation();const go=b.dataset.go;if(go==='Anatomy Lab')return;document.body.classList.remove('dbAnatomyMode');document.documentElement.style.overflowX='';const label=go==='More'?'Profile':go;const candidates=[...document.querySelectorAll('#mobileNav button,#nav button')].filter(x=>getComputedStyle(x).display!=='none');const target=candidates.find(x=>x.textContent.trim().toLowerCase().includes(label.toLowerCase()))||[...document.querySelectorAll('#mobileNav button,#nav button')].find(x=>x.textContent.trim().toLowerCase().includes(label.toLowerCase()));target?.click();},true)})();`;
