const COACH_UI='ui://dback/coach-v5.html';
const VIDEO_UI='ui://dback/video-v2.html';
const Y='https://www.youtube.com/watch?v=';
const TZ='America/New_York';
const COACH_MODE='natural-human';
let DB,PROGRAM;
const day=()=>new Intl.DateTimeFormat('en-US',{timeZone:TZ,weekday:'long'}).format(new Date());
const slug=s=>s.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
const headers=()=>({'access-control-allow-origin':'*','access-control-allow-methods':'GET,POST,DELETE,OPTIONS','access-control-allow-headers':'content-type,accept,mcp-session-id,mcp-protocol-version,last-event-id,authorization','access-control-expose-headers':'mcp-session-id,mcp-protocol-version','cache-control':'no-store','vary':'Origin, Accept'});
async function program(){if(PROGRAM)return PROGRAM;const r=await DB.prepare("SELECT value FROM app_config WHERE key='program'").first();if(!r?.value)throw Error('Workout program is not initialized');return PROGRAM=JSON.parse(r.value)}
async function workout(d=day()){const all=await program(),p=all[d]||all.Monday,x=[];for(const a of p.slice(1)){const id=slug(a[0]),s=await DB.prepare('SELECT * FROM exercise_state WHERE exercise_id=?').bind(id).first();x.push({id,name:a[0],sets:a[1],reps:a[2],rest:a[3],rir:a[4],video:Y+a[5],state:s||null})}const active=await DB.prepare("SELECT * FROM workout_sessions WHERE status='active' ORDER BY started_at DESC LIMIT 1").first();return{type:'workout',day:d,focus:p[0],targetMinutes:60,warmupCount:15,coachMode:COACH_MODE,exercises:x,activeSession:active||null}}
async function findExercise(name){const all=await program(),q=String(name||'').trim().toLowerCase();for(const [d,p] of Object.entries(all))for(const a of p.slice(1)){const n=String(a[0]);if(n.toLowerCase()===q||n.toLowerCase().includes(q)||q.includes(n.toLowerCase()))return{day:d,id:slug(n),name:n,sets:a[1],reps:a[2],rest:a[3],rir:a[4],video:Y+a[5]}}throw Error(`Exercise not found: ${name}`)}
async function start(a={}){const id=crypto.randomUUID(),d=a.day||day(),ts=new Date().toISOString();await DB.prepare('INSERT INTO workout_sessions(id,workout_date,day_name,started_at,readiness,sleep_hours,bodyweight_lb,status) VALUES(?,?,?,?,?,?,?,?)').bind(id,ts.slice(0,10),d,ts,a.readiness??null,a.sleep_hours??null,a.bodyweight_lb??null,'active').run();return{...await workout(d),activeSession:{id,day_name:d,started_at:ts,status:'active'}}}
async function log(a){const ts=new Date().toISOString();await DB.prepare('INSERT INTO set_logs(session_id,exercise_id,set_number,weight_lb,reps,seconds,rir,rpe,discomfort,completed_at) VALUES(?,?,?,?,?,?,?,?,?,?)').bind(a.session_id,a.exercise_id,a.set_number,a.weight_lb??null,a.reps??null,a.seconds??null,a.rir??null,a.rpe??null,a.discomfort??0,ts).run();const p=await DB.prepare('SELECT * FROM exercise_state WHERE exercise_id=?').bind(a.exercise_id).first(),w=a.weight_lb??p?.last_weight_lb??null,r=a.reps??p?.last_reps??null,nw=w;if(w!=null&&r!=null&&(a.rir==null||a.rir>=1))nw=Math.round((w+2.5)*2)/2;await DB.prepare(`INSERT INTO exercise_state(exercise_id,last_weight_lb,last_reps,last_seconds,last_rir,best_weight_lb,best_reps,best_seconds,next_weight_lb,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?) ON CONFLICT(exercise_id) DO UPDATE SET last_weight_lb=excluded.last_weight_lb,last_reps=excluded.last_reps,last_seconds=excluded.last_seconds,last_rir=excluded.last_rir,best_weight_lb=MAX(COALESCE(exercise_state.best_weight_lb,0),COALESCE(excluded.last_weight_lb,0)),best_reps=MAX(COALESCE(exercise_state.best_reps,0),COALESCE(excluded.last_reps,0)),best_seconds=MAX(COALESCE(exercise_state.best_seconds,0),COALESCE(excluded.last_seconds,0)),next_weight_lb=excluded.next_weight_lb,updated_at=excluded.updated_at`).bind(a.exercise_id,w,r,a.seconds??null,a.rir??null,w,r,a.seconds??null,nw,ts).run();return{type:'set_logged',recommendation:nw>w?`Next session target: ${nw} lb if form stays clean.`:'Keep current load and progress reps/time.'}}
async function finish(a){const x=await DB.prepare('SELECT * FROM workout_sessions WHERE id=?').bind(a.session_id).first();if(!x)throw Error('Workout session not found');const end=new Date().toISOString(),dur=Math.max(0,Math.floor((Date.parse(end)-Date.parse(x.started_at))/1000));await DB.prepare("UPDATE workout_sessions SET completed_at=?,duration_seconds=?,session_rpe=?,notes=?,status='complete' WHERE id=?").bind(end,dur,a.session_rpe??null,a.notes??null,a.session_id).run();const s=await DB.prepare('SELECT COUNT(*) count,SUM(COALESCE(weight_lb,0)*COALESCE(reps,0)) volume FROM set_logs WHERE session_id=?').bind(a.session_id).first();return{type:'summary',duration_seconds:dur,sets:+(s?.count||0),volume_lb_reps:+(s?.volume||0)}}
const defs=[
['open_coach','Open DBACK AI Coach','Use this when the user wants to open the interactive DBACK AI workout coach.',{},COACH_UI,true],
['get_today_workout','Get today workout','Use this when the user asks what to train today.',{day:{type:'string'}},COACH_UI,true],
['start_workout','Start tracked workout','Use this when the user is ready to begin a tracked workout.',{day:{type:'string'},readiness:{type:'integer',minimum:1,maximum:10},sleep_hours:{type:'number'},bodyweight_lb:{type:'number'}},COACH_UI,false],
['get_exercise_video','Show exercise instruction video','Use this when the user asks to show, watch, or play the instructional video for an exercise in the same ChatGPT conversation.',{exercise_name:{type:'string'}},VIDEO_UI,true],
['log_set','Log completed set','Use this when the user completes a set and wants it recorded.',{session_id:{type:'string'},exercise_id:{type:'string'},set_number:{type:'integer',minimum:1},weight_lb:{type:'number'},reps:{type:'integer'},seconds:{type:'integer'},rir:{type:'number'},rpe:{type:'number'},discomfort:{type:'integer'}},null,false],
['complete_workout','Complete workout','Use this when the user finishes a workout.',{session_id:{type:'string'},session_rpe:{type:'number'},notes:{type:'string'}},null,false],
['get_history','Get workout history','Use this when the user asks for recent workout history.',{limit:{type:'integer',minimum:1,maximum:30}},null,true]
];
const tools=defs.map(x=>{const meta=x[4]?{ui:{resourceUri:x[4]},'openai/outputTemplate':x[4]}:{};return{name:x[0],title:x[1],description:x[2],inputSchema:{type:'object',properties:x[3],required:x[0]==='get_exercise_video'?['exercise_name']:x[0]==='log_set'?['session_id','exercise_id','set_number']:x[0]==='complete_workout'?['session_id']:[]},annotations:{readOnlyHint:x[5],destructiveHint:false,openWorldHint:false},_meta:meta}});
const out=(x,t='DBACK AI Coach updated.')=>({structuredContent:x,content:[{type:'text',text:t}]});
async function call(n,a={}){if(n==='open_coach'||n==='get_today_workout')return out(await workout(a.day||day()));if(n==='start_workout')return out(await start(a),'Workout started. I’ll coach you one exercise and one set at a time.');if(n==='get_exercise_video'){const e=await findExercise(a.exercise_name);return out({type:'video',exercise:e},`${e.name} instructional video is shown below.`)}if(n==='log_set'){const r=await log(a);return out(r,'Set logged. '+r.recommendation)}if(n==='complete_workout')return out(await finish(a),'Workout complete. I’ll use today’s performance to guide the next session.');if(n==='get_history')return out({type:'history',sessions:(await DB.prepare('SELECT * FROM workout_sessions ORDER BY started_at DESC LIMIT ?').bind(Math.min(a.limit||10,30)).all()).results||[]});throw Error('Unknown tool')}
function coachHtml(){return`<!doctype html><meta name=viewport content="width=device-width,initial-scale=1"><style>body{margin:0;background:#0b1220;color:#f8fafc;font:14px -apple-system,Segoe UI,sans-serif}.a{max-width:820px;margin:auto;padding:16px}.h,.c{background:#111827;border:1px solid #263247;border-radius:15px;padding:14px;margin:10px 0}.h{background:#172554}.m{color:#94a3b8;font-size:12px}h1,h3{margin:5px 0}.g{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin:9px 0}input,button{border:0;border-radius:9px;padding:10px;font-weight:700}input{width:100%;box-sizing:border-box;background:#0f172a;color:white;border:1px solid #334155}button{background:white;color:#111827}.video{background:#2563eb;color:white}.r{display:flex;gap:7px;flex-wrap:wrap}@media(max-width:600px){.g{grid-template-columns:repeat(2,1fr)}}</style><div class=a><b>DBACK AI COACH</b><div class=m>Natural Human Coach Mode</div><div id=r></div></div><script>let raw=window.openai?.toolOutput||{},d=raw.structuredContent||raw||{},s=d.activeSession;async function C(n,a){let z=await window.openai.callTool(n,a||{}),x=z?.structuredContent||z;if(x){if(n==='log_set'){alert(x.recommendation||'Set logged');return x}d=x;s=x.activeSession||s;R()}return x}async function S(){s=(await C('start_workout',{day:d.day,readiness:8})).activeSession}async function L(id,i){if(!s?.id)await S();let q=k=>document.getElementById(k+i).value,a={session_id:s.id,exercise_id:id,set_number:+q('s')||1};if(q('w'))a.weight_lb=+q('w');if(q('p'))a.reps=+q('p');if(q('i'))a.rir=+q('i');await C('log_set',a)}async function F(){if(s?.id){await C('complete_workout',{session_id:s.id,session_rpe:8});alert('Workout complete')}}async function V(name){if(window.openai?.sendFollowUpMessage)return window.openai.sendFollowUpMessage({prompt:'Show the instructional video for '+name+' using DBACK Coach.',scrollToBottom:true});return C('get_exercise_video',{exercise_name:name})}function R(){let r=document.getElementById('r');r.innerHTML='<div class=h><div class=m>'+d.day+'</div><h1>'+d.focus+'</h1><div class=m>60 min · '+(s?.id?'tracking active':'ready')+' · '+(d.warmupCount||15)+' warm-up movements</div><div class=r style="margin-top:10px"><button onclick=S()>Start</button><button onclick=F()>Finish</button></div></div>'+(d.exercises||[]).map((e,i)=>'<div class=c><div class=m>'+e.sets+' × '+e.reps+' · rest '+e.rest+'s · RIR '+e.rir+'</div><h3>'+e.name+'</h3><div class=m>Previous '+(e.state?.last_weight_lb??'—')+' lb × '+(e.state?.last_reps??'—')+' · next '+(e.state?.next_weight_lb??'—')+'</div><div class=g><input id=s'+i+' value=1 type=number><input id=w'+i+' placeholder=lb type=number step=2.5><input id=p'+i+' placeholder=reps type=number><input id=i'+i+' placeholder=RIR type=number></div><div class=r><button onclick="L(\''+e.id+'\','+i+')">Complete Set</button><button class=video onclick="V(\''+e.name.replace(/'/g,"\\'")+'\')">Show Video in Chat</button></div></div>').join('')}R()</script>`}
function videoHtml(){return`<!doctype html><meta name=viewport content="width=device-width,initial-scale=1"><style>body{margin:0;background:#0b1220;color:#f8fafc;font:14px -apple-system,Segoe UI,sans-serif}.a{max-width:760px;margin:auto;padding:14px}.c{background:#111827;border:1px solid #263247;border-radius:15px;padding:14px}.m{color:#94a3b8;font-size:12px}h2{margin:5px 0 10px}.p{position:relative;width:100%;aspect-ratio:16/9;border-radius:12px;overflow:hidden;background:#000}.p iframe{position:absolute;inset:0;width:100%;height:100%;border:0}</style><div class=a><div class=c id=r></div></div><script>const raw=window.openai?.toolOutput||{},d=raw.structuredContent||raw||{},e=d.exercise||d?.structuredContent?.exercise||{};function vid(u){try{return new URL(u).searchParams.get('v')||''}catch{return''}}const v=vid(e.video||''),src=v?'https://www.youtube-nocookie.com/embed/'+encodeURIComponent(v)+'?rel=0&playsinline=1':'';document.getElementById('r').innerHTML='<div class=m>Professional instructional video</div><h2>'+e.name+'</h2><div class=m>'+e.sets+' × '+e.reps+' · rest '+e.rest+'s · RIR '+e.rir+'</div>'+(src?'<div class=p style="margin-top:12px"><iframe src="'+src+'" title="'+e.name+' instructional video" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe></div>':'<p>Video unavailable.</p>')</script>`}
const rpc=(id,result,status=200)=>new Response(JSON.stringify({jsonrpc:'2.0',id,result}),{status,headers:{...headers(),'content-type':'application/json','mcp-protocol-version':'2025-06-18'}});
const err=(id,code,message,status=400)=>new Response(JSON.stringify({jsonrpc:'2.0',id:id??null,error:{code,message}}),{status,headers:{...headers(),'content-type':'application/json'}});
async function mcp(r){let q;try{q=await r.json()}catch{return err(null,-32700,'Parse error')}if(!q||q.jsonrpc!=='2.0'||typeof q.method!=='string')return err(q?.id,-32600,'Invalid Request');const{id,method,params}=q;if(method==='initialize')return rpc(id,{protocolVersion:['2025-11-25','2025-06-18','2025-03-26'].includes(params?.protocolVersion)?params.protocolVersion:'2025-06-18',capabilities:{tools:{listChanged:false},resources:{subscribe:false,listChanged:false}},serverInfo:{name:'DBACK AI Coach',version:'1.5.1'},instructions:'Natural Human Coach Mode is ON by default. Speak like an attentive human personal trainer, not a database or form. During an active workout, keep replies short, conversational, and action-oriented. Give one clear next action at a time, then wait for the user. Naturally acknowledge completed sets and immediately give the appropriate rest, next set, load, rep target, or form cue. Use the user’s wording and energy level without sounding scripted. Do not repeatedly restate the entire workout or dump tables unless the user asks. Ask only questions needed to coach safely or log performance. If the user says next, done, finished, got 8, used 40 pounds, show video, too easy, too hard, or similar shorthand, infer the workout context and continue. Never claim to see exercise form unless the user provides an image or video. If pain or concerning symptoms are reported, stop or modify the movement and respond conservatively. Preserve workout history and progression as the source of truth.'});if(id==null)return new Response(null,{status:202,headers:headers()});if(method==='ping')return rpc(id,{});if(method==='tools/list')return rpc(id,{tools});if(method==='prompts/list')return rpc(id,{prompts:[]});if(method==='resources/templates/list')return rpc(id,{resourceTemplates:[]});if(method==='resources/list')return rpc(id,{resources:[{uri:COACH_UI,name:'DBACK AI Coach',mimeType:'text/html;profile=mcp-app'},{uri:VIDEO_UI,name:'DBACK Exercise Video',mimeType:'text/html;profile=mcp-app'}]});if(method==='resources/read'&&params?.uri===COACH_UI)return rpc(id,{contents:[{uri:COACH_UI,mimeType:'text/html;profile=mcp-app',text:coachHtml(),_meta:{'openai/widgetDescription':'Interactive DBACK workout coach with Natural Human Coach Mode, set tracking, progression, and history.',ui:{prefersBorder:true,csp:{connectDomains:[],resourceDomains:[]}}}}]});if(method==='resources/read'&&params?.uri===VIDEO_UI)return rpc(id,{contents:[{uri:VIDEO_UI,mimeType:'text/html;profile=mcp-app',text:videoHtml(),_meta:{'openai/widgetDescription':'Inline DBACK exercise instructional video player.',ui:{prefersBorder:true,csp:{connectDomains:[],resourceDomains:[],frameDomains:['https://www.youtube-nocookie.com','https://www.youtube.com']}},'openai/widgetCSP':{connect_domains:[],resource_domains:[],frame_domains:['https://www.youtube-nocookie.com','https://www.youtube.com']}}}]});if(method==='tools/call'){try{return rpc(id,await call(params?.name,params?.arguments||{}))}catch(e){return rpc(id,{isError:true,content:[{type:'text',text:e?.message||'Tool failed'}]})}}return err(id,-32601,'Method not found',404)}
export default{async fetch(r,env){DB=env.DB;const u=new URL(r.url);if(r.method==='OPTIONS')return new Response(null,{status:204,headers:headers()});if(u.pathname==='/mcp'){if(r.method==='POST')return mcp(r);return new Response('DBACK AI Coach MCP endpoint is online. Use an MCP client with POST.',{status:405,headers:{...headers(),allow:'POST, OPTIONS'}})}if(u.pathname==='/health')return Response.json({ok:true,service:'DBACK AI Coach',version:'1.5.0',coachMode:COACH_MODE,database:!!env.DB,mcp:'/mcp'},{headers:headers()});if(u.pathname==='/')return new Response(coachHtml(),{headers:{...headers(),'content-type':'text/html;charset=utf-8'}});return new Response('Not found',{status:404,headers:headers()})}}
